/**
 * 结果相关 API：与 benchmark merged 产物一致（真实数据来自后端 JSON）
 */
export {
  fetchMergedSummary,
  fetchBatchCorrectionReport,
  fetchBatchCorrectionMetrics,
  fetchMergedFiles,
  pcaBeforeAfterImageUrl,
  mergedDownloadUrl,
  fetchPcaAfterJson,
} from '@/api/benchmark'
